/**
 * @xgen/ui primitives
 *
 * 이 디렉토리는 shadcn CLI가 생성하는 순수 UI 프리미티브 컴포넌트를 보관한다.
 * shadcn add <component> 명령으로 추가된 파일만 여기에 위치한다.
 *
 * ⚠️ 규칙:
 * - 이 디렉토리의 파일은 shadcn CLI로 관리한다.
 * - XGEN 비즈니스 로직이 포함된 조합 컴포넌트는 ../components/에 작성한다.
 * - Feature에서 Radix UI를 직접 import하지 않는다. 이 프리미티브를 통해서만 사용한다.
 */
